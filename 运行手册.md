# 运行手册

## 环境要求

| 依赖 | 最低版本 | 说明 |
|------|---------|------|
| Python | 3.9+ | 后端运行时 |
| Node.js | 18+ | 前端构建与开发 |
| npm | 9+ | 前端包管理 |
| Redis | 6+ | Celery 消息中间件（可选） |

---

## 一、后端（Django + DRF）

### 1.1 安装依赖

```bash
cd /Users/grandy/Downloads/Code/Python/test_master
pip3 install --user -r requirements.txt
```

### 1.2 数据库迁移

```bash
python3 manage.py migrate
```

### 1.3 创建超级管理员（首次运行）

```bash
python3 manage.py createsuperuser
```

### 1.4 启动后端服务

```bash
python3 manage.py runserver
```

默认监听 `http://127.0.0.1:8000`。

### 1.5 查看 API 文档（OpenAPI）

启动后端后，访问：

- Swagger UI：`http://127.0.0.1:8000/api/docs/`
- ReDoc：`http://127.0.0.1:8000/api/redoc/`
- OpenAPI Schema（JSON）：`http://127.0.0.1:8000/api/schema/`

---

## 二、前端（Vue 3 + Vite + Vuetify）

### 2.1 安装依赖

```bash
cd /Users/grandy/Downloads/Code/Python/test_master/frontend
npm install --legacy-peer-deps
```

> 由于 ESLint 9 与 @typescript-eslint v7 存在 peer dependency 差异，需要加 `--legacy-peer-deps`。

### 2.2 启动开发服务器

```bash
npm run dev
```

默认监听 `http://localhost:3334`，API 请求自动代理到后端 `http://127.0.0.1:8000`。

### 2.3 生产构建

```bash
npm run build
```

产物输出到 `frontend/dist/` 目录。

### 2.4 其他前端命令

| 命令 | 说明 |
|------|------|
| `npm run lint` | ESLint 检查 |
| `npm run lint:fix` | ESLint 自动修复 |
| `npm run format` | Prettier 格式化 |
| `npm run format:check` | Prettier 格式检查 |
| `npm run typecheck` | TypeScript 类型检查 |
| `npm run preview` | 预览生产构建结果 |

---

## 三、Celery 异步任务（可选）

构建计划的异步执行、定时调度等功能依赖 Celery + Redis。如果不启动，系统会自动降级为线程模式，基本功能不受影响。

### 3.1 启动 Redis

```bash
# macOS (Homebrew)
brew services start redis

# Linux
sudo systemctl start redis

# Docker
docker run -d --name redis -p 6379:6379 redis:7-alpine
```

### 3.2 启动 Celery Worker

```bash
cd /Users/grandy/Downloads/Code/Python/test_master
celery -A TestCaseGenerator worker -l info
```

### 3.3 启动 Celery Beat（定时任务调度）

```bash
celery -A TestCaseGenerator beat -l info
```

> Celery Beat 每 60 秒检查一次构建计划的 cron 表达式，自动触发到期的构建任务。

---

## 四、环境变量

可通过环境变量覆盖默认配置：

| 变量名 | 默认值 | 说明 |
|--------|--------|------|
| `SECRET_KEY` | 内置默认值 | Django Secret Key，生产环境务必修改 |
| `DEBUG` | `True` | 调试模式，生产环境设为 `False` |
| `ALLOWED_HOSTS` | `*` | 允许的域名，生产环境按需设置 |
| `DATABASE_URL` | SQLite | 数据库连接地址（支持 PostgreSQL） |
| `CELERY_BROKER_URL` | `redis://localhost:6379/0` | Celery 消息中间件地址 |
| `VITE_API_BASE` | `http://127.0.0.1:8000` | 前端 API 基础地址（仅开发代理） |

---

## 五、快速启动（完整流程）

```bash
# ---- 终端 1：后端 ----
cd /Users/grandy/Downloads/Code/Python/test_master
pip3 install --user -r requirements.txt
python3 manage.py migrate
python3 manage.py runserver

# ---- 终端 2：前端 ----
cd /Users/grandy/Downloads/Code/Python/test_master/frontend
npm install --legacy-peer-deps
npm run dev

# ---- 终端 3：Celery（可选）----
cd /Users/grandy/Downloads/Code/Python/test_master
celery -A TestCaseGenerator worker -l info

# ---- 终端 4：Celery Beat（可选）----
cd /Users/grandy/Downloads/Code/Python/test_master
celery -A TestCaseGenerator beat -l info
```

启动完成后，浏览器访问 **http://localhost:3334** 即可使用。

---

## 六、项目结构概览

```
test_master/
├── TestCaseGenerator/       # Django 项目配置（settings、urls、wsgi）
├── api/                     # 后端应用
│   ├── models.py            # 数据模型
│   ├── views.py             # 视图/接口
│   ├── serializers.py       # 序列化器
│   ├── testing_views.py     # 测试执行相关视图
│   ├── testing_serializers.py # 测试执行相关序列化器
│   ├── jenkins_client.py    # Jenkins API 集成
│   ├── build_tasks.py       # Celery 异步构建任务
│   ├── exporters.py         # 导出功能（Excel/XMind/JSON）
│   └── api_urls.py          # API 路由注册
├── frontend/                # 前端项目
│   ├── src/
│   │   ├── api/             # API 请求模块
│   │   ├── views/           # 页面视图
│   │   ├── layouts/         # 布局组件
│   │   ├── router/          # 路由配置
│   │   ├── store/           # Pinia 状态管理
│   │   ├── plugins/         # Vuetify / i18n 插件
│   │   ├── locales/         # 国际化语言包（中/英）
│   │   └── utils/           # 工具函数（axios 等）
│   ├── package.json
│   └── vite.config.ts
├── requirements.txt         # Python 依赖
├── pyproject.toml           # pytest / ruff 配置
└── manage.py
```

---

## 七、常见问题

**Q: `python` 命令找不到？**
A: macOS 默认使用 `python3`，请将所有 `python` 替换为 `python3`。

**Q: `npm install` 报 peer dependency 错误？**
A: 加 `--legacy-peer-deps` 参数：`npm install --legacy-peer-deps`。

**Q: 后端启动报 `No DATABASE_URL environment variable set`？**
A: 这是一条警告，不影响运行。默认使用 SQLite，如需 PostgreSQL 请设置 `DATABASE_URL` 环境变量。

**Q: Celery 未启动会影响功能吗？**
A: 不会。系统会自动降级为线程模式执行任务，但定时构建和异步日志采集功能需要 Celery。

**Q: 前端页面空白或接口 404？**
A: 确保后端已启动且监听在 `http://127.0.0.1:8000`，前端开发代理默认转发到该地址。
